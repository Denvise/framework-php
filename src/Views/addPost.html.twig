{% extends 'templates/adminTemp.html.twig' %}
{% block admin %}
<h3>Add post </h3>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Ajouter" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}